# Contributing

Keep changes scoped and preserve the route -> schema -> service -> graph/repository boundaries. Run `make format`, `make lint`, `make typecheck`, and `make test` before opening a pull request.

Do not commit `.env` files, provider keys, logs with prompts, or generated secrets.

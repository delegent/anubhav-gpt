# Security

Do not commit secrets. Use environment variables for `GOOGLE_API_KEY` and production database credentials.

Known MVP limitations:

- No authentication.
- No per-user conversation ownership.
- No distributed rate limiting.
- No abuse monitoring.

Report vulnerability findings through the repository owner until a formal disclosure channel is added.
